# GitHub OAuth App settings
VITE_GITHUB_CLIENT_ID=your_github_oauth_client_id
VITE_TOKEN_PROXY_URL=https://your-worker.your-subdomain.workers.dev

# Optional: override the OAuth redirect URI (defaults to site origin + base path)
# VITE_OAUTH_REDIRECT_URI=https://lettucebo.github.io/PromptLibrary/
